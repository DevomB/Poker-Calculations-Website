---
title: exactEquityCardRemovalGradientAsync
---

# `exactEquityCardRemovalGradientAsync`

Non-blocking [`exactEquityCardRemovalGradient`](/docs/reference/api/combinatorics-exact/exact-equity-card-removal-gradient) — same 52-vector gradient with optional cancellation.

<div className="apiSignature">{`exactEquityCardRemovalGradientAsync(heroHoleCards: CardInput, boardCards: CardInput, range: Float64Array | SparseRangeSpec, options?: AsyncOptions): Promise<CardRemovalGradientResult>`}</div>

See [`AsyncOptions`](/docs/reference/types/async-options).

## Import

```js
const poker = require('poker-calculations');
```

## ESM (Node)

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Same as sync when the exact gradient over a full range would block the event loop — use `await` and optional `AbortSignal`.

## How to use

```js
const poker = require('poker-calculations');

const range = new Float64Array(1326);
range.fill(1 / 1326);

const { gradient, baseEquity } = await poker.exactEquityCardRemovalGradientAsync(
  ['Ah', 'Kh'],
  ['Qh', 'Jh', '2c'],
  range,
);

console.log({ baseEquity, gradientLength: gradient.length });
```

## Return type

[`CardRemovalGradientResult`](/docs/reference/types/card-removal-gradient-result).

## See also

[`exactEquityCardRemovalGradient`](/docs/reference/api/combinatorics-exact/exact-equity-card-removal-gradient) · [`AsyncOptions`](/docs/reference/types/async-options) · [`CardRemovalGradientResult`](/docs/reference/types/card-removal-gradient-result)
