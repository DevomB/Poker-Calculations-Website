---
title: exactRangeDominatedComboFraction
---

# `exactRangeDominatedComboFraction`

<div className="apiSignature">See `index.d.ts` in the npm package for the full signature.</div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Share of villain range strictly behind hero on a complete five-card board.

More walkthroughs: [Tournament, runouts, and bots](/docs/guides/tournament-runouts-and-bots).

## How to use

```js
// poker.exactRangeDominatedComboFraction(...)
```

See the [API index](/docs/reference/api) and [`index.d.ts`](https://github.com/DevomB/Poker-Calculations/blob/main/index.d.ts) for parameters and return shapes.
