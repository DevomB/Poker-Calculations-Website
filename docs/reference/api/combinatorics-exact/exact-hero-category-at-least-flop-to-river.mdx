---
title: exactHeroCategoryAtLeastFlopToRiver
sidebar_position: 40
---

# `exactHeroCategoryAtLeastFlopToRiver`

P(category ≥ threshold) by river.

Card lists use `CardInput` (`string[]` or packed `Uint8Array`). See [Card notation](/docs/concepts/card-notation).

<div className="apiSignature"><code>exactHeroCategoryAtLeastFlopToRiver(heroHoleCards: CardInput, flopThree: CardInput, knownDead: CardInput, minCategoryOrder: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need p(category ≥ threshold) by river in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.exactHeroCategoryAtLeastFlopToRiver(/* see signature above */);
console.log(result);
```

## See also

[`exact-hero-category-joint-flop-to-river`](/docs/reference/api/combinatorics-exact/exact-hero-category-joint-flop-to-river)
