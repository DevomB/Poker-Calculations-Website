---
title: straightMadeFlopToRiverExactProbabilityAsync
sidebar_position: 8
---

# `straightMadeFlopToRiverExactProbabilityAsync`

<div className="apiSignature">{`straightMadeFlopToRiverExactProbabilityAsync(heroHoleCards: CardInput, flopThree: CardInput, knownDead: CardInput, options?: AsyncOptions): Promise<number>`}</div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Async sibling of [`straightMadeFlopToRiverExactProbability`](./straight-made-flop-to-river-exact-probability). Optional [`AsyncOptions`](/docs/reference/types/async-options) for `AbortSignal` cancellation.

## How to use

```js
const poker = require('poker-calculations');

const p = await poker.straightMadeFlopToRiverExactProbabilityAsync(hole, flop, dead);

const ac = new AbortController();
const pending = poker.straightMadeFlopToRiverExactProbabilityAsync(hole, flop, dead, {
  signal: ac.signal,
});
// ac.abort(); // Promise rejects with AbortError
```

## See also

[`straightMadeFlopToRiverExactProbability`](./straight-made-flop-to-river-exact-probability) · [`AsyncOptions`](/docs/reference/types/async-options)
