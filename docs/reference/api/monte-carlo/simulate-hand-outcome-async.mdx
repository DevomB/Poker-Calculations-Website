---
title: simulateHandOutcomeAsync
sidebar_position: 5
---

# `simulateHandOutcomeAsync`

<div className="apiSignature">{`simulateHandOutcomeAsync(holeCards: CardInput, board: CardInput, numSimulations: number, seed: number, villains?: number, options?: AsyncOptions): Promise<number>`}</div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Same as [`simulateHandOutcome`](./simulate-hand-outcome), but the work runs on the libuv thread pool so your event loop stays responsive—HTTP servers, Electron, and batch UIs.

Pass optional [`AsyncOptions`](/docs/reference/types/async-options) to cancel long runs via `AbortSignal`.

## How to use

```js
const poker = require('poker-calculations');

const equity = await poker.simulateHandOutcomeAsync(['Ah', 'Kh'], ['Qh', 'Jh', '2c'], 8000, 42, 1);

const ac = new AbortController();
const pending = poker.simulateHandOutcomeAsync(
  ['Ah', 'Kh'],
  ['Qh', 'Jh', '2c'],
  500_000,
  42,
  1,
  { signal: ac.signal },
);
// ac.abort(); // Promise rejects with AbortError
```

## See also

[`simulateHandOutcome`](./simulate-hand-outcome) · [Monte Carlo equity guide](/docs/guides/monte-carlo-equity) · [`AsyncOptions`](/docs/reference/types/async-options)
