---
title: normalizedRangeWeightSum
sidebar_position: 6
---

# `normalizedRangeWeightSum`

Sum of range weights.

<div className="apiSignature">{`normalizedRangeWeightSum(weights: F64VectorInput): number`}</div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Sum of dense range weights — sanity check before normalizing or feeding subgame solvers.

## How to use

```js
const weights = new Float64Array([0.5, 0, 1.2, 0.3]);
console.log(poker.normalizedRangeWeightSum(weights)); // 2.0
```

## See also

[`materialize-villain-range-after-blockers`](/docs/reference/api/subgame-theory/materialize-villain-range-after-blockers)
