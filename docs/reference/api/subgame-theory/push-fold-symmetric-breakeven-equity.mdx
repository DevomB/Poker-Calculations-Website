---
title: pushFoldSymmetricBreakevenEquity
sidebar_position: 40
---

# `pushFoldSymmetricBreakevenEquity`

Breakeven equity for symmetric jam.

<div className="apiSignature"><code>pushFoldSymmetricBreakevenEquity(jamStackChips: number, deadMoneyChips: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need breakeven equity for symmetric jam in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.pushFoldSymmetricBreakevenEquity(/* see signature above */);
console.log(result);
```

## See also

[`push-fold-symmetric-ev`](/docs/reference/api/subgame-theory/push-fold-symmetric-ev)
