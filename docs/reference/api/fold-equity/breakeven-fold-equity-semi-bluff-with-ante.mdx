---
title: breakevenFoldEquitySemiBluffWithAnte
sidebar_position: 40
---

# `breakevenFoldEquitySemiBluffWithAnte`

Semi-bluff fold equity with ante.

<div className="apiSignature"><code>breakevenFoldEquitySemiBluffWithAnte(potBeforeHeroBet: number, heroBetSize: number, equityWhenCalled: number, totalPotIfCalled: number, anteToPost: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need semi-bluff fold equity with ante in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.breakevenFoldEquitySemiBluffWithAnte(/* see signature above */);
console.log(result);
```

## See also

[`breakeven-fold-equity-semi-bluff`](/docs/reference/api/fold-equity/breakeven-fold-equity-semi-bluff)
