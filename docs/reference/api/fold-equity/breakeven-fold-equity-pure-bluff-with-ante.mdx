---
title: breakevenFoldEquityPureBluffWithAnte
sidebar_position: 40
---

# `breakevenFoldEquityPureBluffWithAnte`

Pure-bluff fold equity with ante in won pot.

<div className="apiSignature"><code>breakevenFoldEquityPureBluffWithAnte(potBeforeHeroBet: number, heroBetOrCallSize: number, anteToPost: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need pure-bluff fold equity with ante in won pot in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.breakevenFoldEquityPureBluffWithAnte(/* see signature above */);
console.log(result);
```

## See also

[`breakeven-fold-equity-pure-bluff`](/docs/reference/api/fold-equity/breakeven-fold-equity-pure-bluff)
