---
title: valueToBluffRatioWithRake
sidebar_position: 40
---

# `valueToBluffRatioWithRake`

Reciprocal bluff:value with rake.

<div className="apiSignature"><code>valueToBluffRatioWithRake(potBeforeBet: number, betSize: number, rakeFraction: number, rakeCap: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need reciprocal bluff:value with rake in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.valueToBluffRatioWithRake(/* see signature above */);
console.log(result);
```

## See also

[`bluff-to-value-ratio-with-rake`](/docs/reference/api/gto-frequencies/bluff-to-value-ratio-with-rake) · [`value-to-bluff-ratio`](/docs/reference/api/gto-frequencies/value-to-bluff-ratio)
