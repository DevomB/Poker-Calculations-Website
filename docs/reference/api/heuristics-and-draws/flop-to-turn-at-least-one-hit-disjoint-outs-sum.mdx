---
title: flopToTurnAtLeastOneHitDisjointOutsSum
sidebar_position: 40
---

# `flopToTurnAtLeastOneHitDisjointOutsSum`

Sum disjoint outs, then one-street hit formula.

<div className="apiSignature"><code>flopToTurnAtLeastOneHitDisjointOutsSum(unseen: number, outsPerCategory: number[]): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need sum disjoint outs, then one-street hit formula in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.flopToTurnAtLeastOneHitDisjointOutsSum(/* see signature above */);
console.log(result);
```

## See also

[`flop-to-turn-at-least-one-hit-probability`](/docs/reference/api/heuristics-and-draws/flop-to-turn-at-least-one-hit-probability)
