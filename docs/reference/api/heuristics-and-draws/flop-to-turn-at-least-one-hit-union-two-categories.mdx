---
title: flopToTurnAtLeastOneHitUnionTwoCategories
sidebar_position: 40
---

# `flopToTurnAtLeastOneHitUnionTwoCategories`

Two overlapping categories, one street (flop→turn).

<div className="apiSignature"><code>flopToTurnAtLeastOneHitUnionTwoCategories(unseen: number, outsA: number, outsB: number, sharedAb: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need two overlapping categories, one street (flop→turn) in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.flopToTurnAtLeastOneHitUnionTwoCategories(/* see signature above */);
console.log(result);
```

## See also

[`flop-to-turn-at-least-one-hit-probability`](/docs/reference/api/heuristics-and-draws/flop-to-turn-at-least-one-hit-probability) · [`flop-to-river-at-least-one-hit-union-two-categories`](/docs/reference/api/heuristics-and-draws/flop-to-river-at-least-one-hit-union-two-categories)
