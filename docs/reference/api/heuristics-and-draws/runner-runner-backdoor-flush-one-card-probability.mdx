---
title: runnerRunnerBackdoorFlushOneCardProbability
sidebar_position: 40
---

# `runnerRunnerBackdoorFlushOneCardProbability`

One-card backdoor flush probability.

<div className="apiSignature"><code>runnerRunnerBackdoorFlushOneCardProbability(suitCardsRemaining: number, unseenCards: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need one-card backdoor flush probability in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.runnerRunnerBackdoorFlushOneCardProbability(/* see signature above */);
console.log(result);
```

## See also

[`runner-runner-backdoor-flush-two-card-probability`](/docs/reference/api/heuristics-and-draws/runner-runner-backdoor-flush-two-card-probability)
