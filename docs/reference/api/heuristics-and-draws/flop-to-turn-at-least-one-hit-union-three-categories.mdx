---
title: flopToTurnAtLeastOneHitUnionThreeCategories
sidebar_position: 40
---

# `flopToTurnAtLeastOneHitUnionThreeCategories`

Three categories with inclusion–exclusion, one street.

<div className="apiSignature"><code>flopToTurnAtLeastOneHitUnionThreeCategories(unseen: number, outsA: number, outsB: number, outsC: number, sharedAb: number, sharedAc: number, sharedBc: number, sharedAbc: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need three categories with inclusion–exclusion, one street in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.flopToTurnAtLeastOneHitUnionThreeCategories(/* see signature above */);
console.log(result);
```

## See also

[`flop-to-turn-at-least-one-hit-union-two-categories`](/docs/reference/api/heuristics-and-draws/flop-to-turn-at-least-one-hit-union-two-categories)
