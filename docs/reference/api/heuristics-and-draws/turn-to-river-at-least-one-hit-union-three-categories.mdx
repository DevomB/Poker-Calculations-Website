---
title: turnToRiverAtLeastOneHitUnionThreeCategories
sidebar_position: 40
---

# `turnToRiverAtLeastOneHitUnionThreeCategories`

Three categories, turn→river.

<div className="apiSignature"><code>turnToRiverAtLeastOneHitUnionThreeCategories(unseen: number, outsA: number, outsB: number, outsC: number, sharedAb: number, sharedAc: number, sharedBc: number, sharedAbc: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need three categories, turn→river in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.turnToRiverAtLeastOneHitUnionThreeCategories(/* see signature above */);
console.log(result);
```

## See also

[`turn-to-river-at-least-one-hit-union-two-categories`](/docs/reference/api/heuristics-and-draws/turn-to-river-at-least-one-hit-union-two-categories)
