---
title: breakevenRaiseEquity
sidebar_position: 40
---

# `breakevenRaiseEquity`

Equity needed for neutral raise.

<div className="apiSignature"><code>breakevenRaiseEquity(potBeforeRaise: number, raiseSize: number, foldEquity: number, potIfCalled: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need equity needed for neutral raise in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.breakevenRaiseEquity(/* see signature above */);
console.log(result);
```

## See also

[`expected-value-raise`](/docs/reference/api/pot-and-ev/expected-value-raise) · [`breakeven-fold-equity-semi-bluff`](/docs/reference/api/fold-equity/breakeven-fold-equity-semi-bluff)
