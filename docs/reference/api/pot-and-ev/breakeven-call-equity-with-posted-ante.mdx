---
title: breakevenCallEquityWithPostedAnte
sidebar_position: 40
---

# `breakevenCallEquityWithPostedAnte`

Breakeven when hero still posts ante.

<div className="apiSignature"><code>breakevenCallEquityWithPostedAnte(potBeforeCall: number, toCall: number, anteToPost: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need breakeven when hero still posts ante in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.breakevenCallEquityWithPostedAnte(/* see signature above */);
console.log(result);
```

## See also

[`breakeven-call-equity`](/docs/reference/api/pot-and-ev/breakeven-call-equity) · [`orbit-cost-chips`](/docs/reference/api/stacks-and-display/orbit-cost-chips)
