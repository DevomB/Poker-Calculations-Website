---
title: API overview
slug: /reference/api
---

import Link from '@docusaurus/Link';
import {
	apiReferenceCategories,
	apiReferenceTotalExports,
} from '@site/src/data/api-reference';

# API reference

**{apiReferenceTotalExports}** native functions, grouped below. Each page includes **Import**, **When to use**, and **How to use**.

<table>
	<thead>
		<tr>
			<th>Category</th>
			<th>Exports</th>
		</tr>
	</thead>
	<tbody>
		{apiReferenceCategories.map((category) => (
			<tr key={category.to}>
				<td>
					<Link to={category.to}>{category.title}</Link>
				</td>
				<td>{category.count}</td>
			</tr>
		))}
	</tbody>
</table>

[TypeScript types](/docs/reference/types) for `decideAction`, batch specs, and result objects. [`AsyncOptions`](/docs/reference/types/async-options) documents optional `AbortSignal` cancellation on all `*Async` exports.

## `poker-calculations/encode` subpath

Six pure-JS helpers (`packCards`, `unpackCards`, `cardStringToDeckIndex`, `deckIndexToCardString`, `packPokerState`, `unpackPokerState`) ship on the optional subpath—no extra native exports. See [Packed card input](/docs/concepts/packed-card-input#helpers-poker-calculationsencode) and [Packed poker state](/docs/concepts/packed-poker-state#encodejs-helpers).
