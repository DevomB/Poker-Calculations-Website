---
title: normalizeBotConfig
sidebar_position: 11
---
# `normalizeBotConfig`
Fill and clamp bot configuration fields to native defaults.
<div className="apiSignature">{`normalizeBotConfig(config: Partial<NativeBotConfig>): NativeBotConfig`}</div>

## Import
```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use
Use `normalizeBotConfig` when you need to fill and clamp bot configuration fields to native defaults.

## How to use
```js
const poker = require('poker-calculations');
const state = {
  players: [
    {holeCards: ['Ah', 'Kh'], stack: 200, seat: 0, committedThisStreet: 0},
    {holeCards: ['7c', '7d'], stack: 180, seat: 1, folded: false},
  ],
  communityCards: ['Qh', 'Jh', '2c'],
  phase: 'flop',
  pot: 30,
  currentBet: 10,
  smallBlind: 1,
  bigBlind: 2,
  actingIndex: 0,
  actedThisStreet: [false, true],
};

const result = poker.normalizeBotConfig(state, {monteCarloSimulations: 0});
console.log(result);
```

## See also
[`decideAction`](/docs/reference/api/strategy/decide-action) · [`NativePokerState`](/docs/reference/types/native-poker-state)


