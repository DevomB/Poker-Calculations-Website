---
title: betSizingIndifferencePoint
sidebar_position: 3
---
# `betSizingIndifferencePoint`
Solve a simple bet-size indifference point from fold frequency and called equity.
<div className="apiSignature">{`betSizingIndifferencePoint(pot: number, foldFrequency: number, equityWhenCalled: number): number`}</div>

## Import
```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use
Use `betSizingIndifferencePoint` when you need to solve a simple bet-size indifference point from fold frequency and called equity.

## How to use
```js
const poker = require('poker-calculations');
const range = new Float64Array(1326);
range.fill(1 / 1326);

const result = poker.betSizingIndifferencePoint(range, range, ['Qh', 'Jh', '2c'], 100, [33, 66, 100]);
console.log(result);
```

## See also
[`betAsPotFraction`](/docs/reference/api/sizing-and-commitment/bet-as-pot-fraction) · [`expectedValueRaise`](/docs/reference/api/pot-and-ev/expected-value-raise)


