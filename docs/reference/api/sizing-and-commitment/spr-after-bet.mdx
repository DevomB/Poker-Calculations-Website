---
title: sprAfterBet
sidebar_position: 40
---

# `sprAfterBet`

SPR after hero bets heads-up.

<div className="apiSignature"><code>sprAfterBet(potBeforeBet: number, betSize: number, effectiveStackBeforeBet: number): number</code></div>

## Import

```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use

Use when you need spr after hero bets heads-up in trainers, study tools, or backend scripts. Values follow the native C++ implementation in `poker-calculations`.

## How to use

```js
const poker = require('poker-calculations');

const result = poker.sprAfterBet(/* see signature above */);
console.log(result);
```

## See also

[`spr-after-call`](/docs/reference/api/sizing-and-commitment/spr-after-call) · [`spr`](/docs/reference/api/stacks-and-display/spr) · [`commitment-ratio-after-bet`](/docs/reference/api/sizing-and-commitment/commitment-ratio-after-bet)
