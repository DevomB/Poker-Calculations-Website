---
title: villainCappedRangeScore
---
# `villainCappedRangeScore`
Score how capped a villain range appears on the board.
<div className="apiSignature">{`villainCappedRangeScore(range: SparseRangeSpec | Float64Array, board: CardInput): number`}</div>
See [Packed card input](/docs/concepts/packed-card-input) and [SparseRangeSpec](/docs/reference/types/sparse-range-spec) where those inputs apply.

## Import
```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use
Use `villainCappedRangeScore` when you need to score how capped a villain range appears on the board.

## How to use
```js
const poker = require('poker-calculations');
const result = poker.villainCappedRangeScore(1, 1, 7, 5);
console.log(result);
```

## See also
[`bayesianRangeUpdateFromAction`](/docs/reference/api/subgame-theory/bayesian-range-update-from-action) · [`NativeOpponentModel`](/docs/reference/types/native-opponent-model)


