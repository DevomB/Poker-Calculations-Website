---
title: opponentRangeElasticityFromSizing
---
# `opponentRangeElasticityFromSizing`
Estimate how continue rate changes as sizing changes.
<div className="apiSignature">{`opponentRangeElasticityFromSizing(sizes: F64VectorInput, continueRates: F64VectorInput): number`}</div>

## Import
```js
const poker = require('poker-calculations');
```

ESM (Node):

```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const poker = require('poker-calculations');
```

## When to use
Use `opponentRangeElasticityFromSizing` when you need to estimate how continue rate changes as sizing changes.

## How to use
```js
const poker = require('poker-calculations');
const result = poker.opponentRangeElasticityFromSizing(1, 1, 7, 5);
console.log(result);
```

## See also
[`bayesianRangeUpdateFromAction`](/docs/reference/api/subgame-theory/bayesian-range-update-from-action) · [`NativeOpponentModel`](/docs/reference/types/native-opponent-model)


