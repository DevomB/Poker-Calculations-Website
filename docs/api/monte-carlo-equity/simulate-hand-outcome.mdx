---
title: simulateHandOutcome
sidebar_label: simulateHandOutcome
description: "simulateHandOutcome"
---

## Summary

Native export `simulateHandOutcome` from the `poker-calculations` addon.

## Signature

```ts
simulateHandOutcome(
    holeCards: string[],
    board: string[],
    numSimulations: number,
    seed: number,
    villains?: number
  ): number;
```

## Implementation notes

```text
_See TypeScript types in the repository._
```

## When to use

Use when you need equity estimates against random or uniform villains—either fast single-threaded sampling or threaded batches, or exact HU numbers on a fixed board.

**Concrete trigger:** See the summary below.

**Skip this export** when you need full multi-street game trees, opponent-specific ranges, or browser-side execution without the native addon—this library is Node + N-API.

## Example

```js
const poker = require('poker-calculations');
// simulateHandOutcome(...) — see signature above
```

Runnable sample in the repo: [NPM/examples/monte-carlo-equity.mjs](https://github.com/DevomB/Poker-Calculations/blob/main/NPM/examples/monte-carlo-equity.mjs).

## See also

- [API index](/docs/api)
- Type definitions: [`NPM/index.d.ts`](https://github.com/DevomB/Poker-Calculations/blob/main/NPM/index.d.ts)
