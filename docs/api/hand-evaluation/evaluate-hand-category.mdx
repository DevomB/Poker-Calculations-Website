---
title: evaluateHandCategory
sidebar_label: evaluateHandCategory
description: "Category label only (`highCard` … `royalFlush`); see [Hand rank labels](/docs/intro#hand-rank-labels)."
---

## Summary

Category label only (`highCard` … `royalFlush`); see [Hand rank labels](/docs/intro#hand-rank-labels).

## Signature

```ts
evaluateHandCategory(holeCards: string[], board: string[]): string;
```

## Implementation notes

```text
_See TypeScript types in the repository._
```

## When to use

Use these once cards are validated: resolving best five from up to seven, comparing spots, or bucketing into categories for reports. They are deterministic and do not depend on stack or pot.

**Concrete trigger:** Category label only (`highCard` … `royalFlush`); see [Hand rank labels](/docs/intro#hand-rank-labels).

**Skip this export** when you need full multi-street game trees, opponent-specific ranges, or browser-side execution without the native addon—this library is Node + N-API.

## Example

```js
const poker = require('poker-calculations');
// evaluateHandCategory(...) — see signature above
```

Browse grouped samples under [`NPM/examples/`](https://github.com/DevomB/Poker-Calculations/tree/main/NPM/examples).

## See also

- [API index](/docs/api)
- Type definitions: [`NPM/index.d.ts`](https://github.com/DevomB/Poker-Calculations/blob/main/NPM/index.d.ts)
