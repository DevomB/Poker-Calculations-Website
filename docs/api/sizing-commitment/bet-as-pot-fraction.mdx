---
title: betAsPotFraction
sidebar_label: betAsPotFraction
description: "betAsPotFraction"
---

## Summary

Native export `betAsPotFraction` from the `poker-calculations` addon.

## Signature

```ts
betAsPotFraction(potBeforeBet: number, betSize: number): number;
```

## Implementation notes

```text
_See TypeScript types in the repository._
```

## When to use

Use to express bets as pot fractions, SPR after a single HU call, or how much of the effective stack a call consumes—common in commitment and stack-depth discussions.

**Concrete trigger:** See the summary below.

**Skip this export** when you need full multi-street game trees, opponent-specific ranges, or browser-side execution without the native addon—this library is Node + N-API.

## Example

```js
const poker = require('poker-calculations');
// betAsPotFraction(...) — see signature above
```

Runnable sample in the repo: [NPM/examples/sizing-commitment.mjs](https://github.com/DevomB/Poker-Calculations/blob/main/NPM/examples/sizing-commitment.mjs).

## See also

- [API index](/docs/api)
- Type definitions: [`NPM/index.d.ts`](https://github.com/DevomB/Poker-Calculations/blob/main/NPM/index.d.ts)
